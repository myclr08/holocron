fastapi==0.115.6
uvicorn==0.34.0
requests==2.32.3
openpyxl==3.1.5
cryptography==44.0.0
# Outlook COM (Asama 7) ve kilitli dosyayi paylasimli okuma (win32file, Teams
# arama gecmisi): yalnizca Windows'ta kurulur, Linux paketine girmez.
pywin32==312; sys_platform == "win32"
# Gorusme notlari (Asama 12): WASAPI dongu kaydi. Yalnizca Windows'ta kurulur.
pyaudiowpatch==0.2.12.7; sys_platform == "win32"
# Windows bildirimi (istege bagli; yoksa bildirim sessizce atlanir).
winotify==1.1.0; sys_platform == "win32"
